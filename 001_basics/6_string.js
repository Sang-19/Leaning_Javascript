// ` - the button is in below esc not semicolon backticks - more readability working 
const na = "hel"
console.log(`hello my name is ${na.toUpperCase()} and my repo count is ${na}`)

const gameNew = new String ('hoites')

console.log(gameNew.__proto__)  // to accesss the prototype

// . function sei kuch bhi call kiya ja sakta hai 

// charAt(2) - to know which element in which character 
//indexOf

// strings ke bare mai notes - explanantory video 

// slice -- negative value , substring - no negative value like reverse the strinnng 

// trim() - removes the starting and ending spaces in a string 

